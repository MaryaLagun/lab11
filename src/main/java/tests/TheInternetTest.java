package tests;

import basepage.HomePage;
import org.junit.Assert;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.DataProvider;

public class TheInternetTest extends BaseTest{
  //  private static final String EXPECTED_URL="https://the-internet.herokuapp.com/abtest";

 //   HomePage homePage=new HomePage();
 //   @Test
 //   public void locatorTest()
 //   {
  //      homePage.openURL();
 //       homePage.clickABTestingLink("A/B Testing");
   //     String currentUrl=driver.getCurrentUrl();
 //       Assert.assertEquals(currentUrl,EXPECTED_URL);
   // }

    //@DataProvider
 //   public Object[][] prepareDate{
 //       return new Object[][]{{"A/B Testing","https://the-internet.herokuapp.com/abtest"},{"Add/Remove Elements","https://the-internet.herokuapp.com/add_remove_elements"}};
 //   }

   // private final static String EXPECTED_URL="https://the-internet.herokuapp.com/abtest";
 //   @Test(dataProvider="prepareDate")
   // public void locatorTest(String link,String expectedURL){
  //      HomePage homePage=new HomePage(driver);
   //     homePage.clickLink(link);
   //     String actualUrl=driver.getCurrentUrl();
    //    Assert.assertTrue(actualUrl.equals(expectedURL));

       //WebDriver driver = new ChromeDriver();
  //      driver.get("https://the-internet.herokuapp.com/abtest");
   //     driver.manage().window().maximize();
    //    WebElement abtestLink=driver.findElement(By.xpath("//ul/li/a[@href='/abtest']"));
   //     abtestLink.click();
   //     String actualUrl=driver.getCurrentUrl();
   //     Assert.assertTrue(actualUrl.equals(EXPECTED_URL));
    }

//}
